<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FitnessKit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FitnessKit" parent="Base.Theme.FitnessKit" />

    <!--  BottomNav Style Custom  -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">#00EEEFF4</item>
    </style>

    <style name="bottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemTextAppearanceActive">@style/ActiveText</item>
        <item name="itemTextAppearanceInactive">@style/InactiveText</item>
    </style>

    <style name="ActiveText">
        <item name="android:textColor">#0055FF</item>
    </style>

    <style name="InactiveText">
        <item name="android:textColor">@color/gray</item>
    </style>
</resources>